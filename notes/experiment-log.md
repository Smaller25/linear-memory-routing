# Experiment log — linear-memory-routing

| date | phase | what | status |
|------|-------|------|--------|
| 2026-06-15 | 0 (scaffold) | MC library (`lmr/`): ssd_scan, segment_runner, readout (RM/GRM/SSC), converter, MoM adapter, MQAR/passkey gens, scripts. CPU tests for scan linearity, RM≡vanilla@N=1, runner==FLA forward, readout shapes. | code complete; CPU tests green; GPU experiments pending local A100 |
| 2026-06-15 | 0 (A100 validate) | Ran GPU tests (14 pass, 1 non-blocking MoM-arm fail) + converter logit-match (FLA≡mamba_ssm: argmax 100%, r=1.0) + Phase-0 baseline. Fixed 3 bugs: `_tied_weights_keys` list→dict (transformers 5.12), vocab pad 50277→50288, atol 1e-3→1e-2. **Training-free MC-RM neutral-to-negative** (mqar Δ0; passkey@2048 0.253→0.022; chunk64 MQAR Δ−0.081) — frozen model not trained for segmented/summed readout + synthetic tasks OOD for pretrained LM. → Phase 1 (train GRM/SSC) + real-text passkey. | verified; see `report/0002.md` |
| 2026-06-15 | 0b (in-dist) | Natural-language passkey (`make_text_passkey`, gpt-neox tok, right-pad). **Vanilla solves ~100% to 4k**; training-free MC-RM degrades monotonically with #segments: 512 −0.007 / 1024 −0.014 / 2048 −0.220 / 4096 **−0.801**. Clean confirmation that blind cached-state summation on a frozen model breaks recall. | see `report/0002.md` |
| 2026-06-15 | 1 (train GRM) | Trained per-layer GRM gate (`W_u`, 805M params; backbone frozen) on multi-segment passkey (train_len 1024, batch 4, 200 steps, lr 1e-3). Fix: `D.contiguous()` for mamba_ssm backward. **GRM recovers RM's collapse**: 2048 RM 0.841→GRM 0.993 (=vanilla), 4096 RM 0.209→GRM 0.482 (>2×). Limits: only *matches* vanilla (passkey ceiling, no headroom), and length-gen gap at 4096 (trained at 4 seg). → train longer-ctx + eval where vanilla fails. | see `report/0003.md` |
| 2026-06-15 | 1b (longer-ctx) | Memory-light eval (score labelled positions only) unblocks 8k–16k. Retrained GRM @train_len 2048 (8 seg). 4096 (16 seg) generalization improved 0.522→**0.712**; but 8192 (32 seg) still 0.000. GRM generalizes only ~2× trained segment count. vanilla@8192=0.738 (headroom exists). → SSC top-k routing (constant fan-in) or curriculum lengths; eval where vanilla collapses. | see `report/0004.md` |
| 2026-06-17 | 1c (SSC) | **SSC top-k routing — first NET WIN over vanilla.** Trained @train_len 2048 (8 seg, topk 2), same setting as GRM. 8192 (32 seg): vanilla 0.738, GRM 0.000, **SSC 0.986 (+0.248 over vanilla)**; 4096: SSC 0.971 vs GRM 0.712. Constant top-k fan-in generalizes across segment counts where GRM's grow-with-N summation collapses. Frozen backbone + trained router beats the fixed state where it fails. Caveats: 805M router (low-rank TODO), large load-balance aux (ablate). | see `report/0005.md` |
| 2026-06-17 | A+B (GDN + new heads) | Refactored runner behind a per-arch **adapter** (boundary = *block*, since GDN blocks add an MLP); mamba2 math moved verbatim. Added **GDN backbone** (memory isolated by zeroing `v` — affine-in-state, CPU-proven in `test_gdn_superposition.py`), **MoMReadout** (M bounded slots + shared slot + Switch aux), **AttentionOverMemories**, a **`low_rank_dim`** option on every trained head (~25× fewer params; full-rank keeps legacy keys), and runner-side **HierarchicalCache** (`--hierarchical-k`). New `lmr/loaders.py` (`load_backbone`, GDN tokenizer ≠ gpt-neox); scripts gained `--arch/--variant{...,mom,aom}/--low-rank-dim/--hierarchical-k`. CPU helper/head tests + GPU GDN runner≡forward / RM≡vanilla gates added. | code complete; **tests not run here (no torch on planning box)** — run on A100, then GDN sweep; see `report/0006.md` |
| 2026-06-17 | A+B (A100 validate) | GPU gates pass (GDN run_block≡forward, RM≡vanilla@N=1, mamba2 regression clean; 29 pass, 1 pre-existing MoM-arm fail). GDN load fixes: tied_weights list→dict, MLP fused gate_proj→gate+up split, drop obsolete attn.D, Mistral tokenizer. **GDN-1.3b far more long-ctx-robust than mamba2**: vanilla passkey 8192=0.93 (mamba2 0.738), collapses only at 16384=0.771 / 32768=0.594. MC net-win test must target 16k/32k. SSC-GDN training next. | see `report/0006.md` |
| 2026-06-17 | A+B (GDN blocker) | GDN router TRAINING blocked on A100: `chunk_gated_delta_rule` backward needs 225KB shared mem > A100 166KB (GDN head_dim=256, H100-trained ckpt). GDN forward/inference fine (vanilla+RM eval OK); only trained heads on GDN blocked. Options: H100 / smaller BT / fused_recurrent bwd / GDN-as-RM-only + train routers on mamba2 sizes. | see `report/0006.md` |
| 2026-06-17 | C (size scaling) | mamba2 **2.7b** SSC (low-rank 64, train@2048): 8192 vanilla 0.50 → **SSC 1.00 (+0.50)**; 4096 0.942→0.978; RM collapses (8192=0.00). **Net win grows with size** (1.3b was +0.248 @8k → 2.7b +0.50). Low-rank routers (~25× fewer params: 805M→31M @1.3b, ~1.7B→52M @2.7b) suffice. 130m too small/flat + loose converter logit-match → not a clean testbed, skipped. | see `report/0007.md` |
| 2026-06-17 | B (mechanism cmp) | mamba2-1.3b, all heads low-rank64 @train2048. **Only SSC generalizes to 8192** (+0.248): SSC 0.986 vs AoM 0.014, MoM 0.053, GRM 0.000, RM 0.000. 4096: SSC 0.971 > MoM 0.820 > GRM 0.712 > AoM 0.655 > RM 0.209. Hard top-k sparsity avoids distractor-dilution; dense (AoM) / merge (MoM) lose the needle as #segments grows. → hierarchical caching (bound N) next. | see `report/0008.md` |

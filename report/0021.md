# 0021 — Multi-key failure confirmed on the real frozen model; mechanism localization (2026-07-09)

Investigating **why frozen SSC fails multi-key** (hypotheses in `notes/descriptor-hypotheses.md`). The
long-gdn frozen GDN-1.3B + trained-SSC + RULER infra is not in this environment (checkpoints/data are
gitignored, on the campaign machine). But the standalone repo already has a working frozen-SSC pipeline
from the 0009/0014 session: `state-spaces/mamba2-370m` (cached) + trained head `ckpt/ssc_370m.pt`
(low-rank 64, chunk 256, top-k 2) + `data/ruler/*/{niah_single_1,niah_multikey_2}`. So we measured
multi-key on the **real frozen model here**.

## Result — real frozen mamba2-370m, RULER (max-examples 50)
| task | length | vanilla | +RM | **+SSC** |
|---|---|---|---|---|
| niah_single_1 | 2048 | 0.691 | 0.681 | **0.734** (+0.04) |
| niah_single_1 | 4096 | 0.695 | 0.105 | **0.779** (+0.08) |
| niah_multikey_2 | 2048 | **0.452** | 0.419 | **0.129** (−0.32) |
| niah_multikey_2 | 4096 | 0.165 | 0.072 | 0.113 (−0.05) |

**SSC helps single-needle and HURTS multi-key** — vanilla 0.452 → +SSC 0.129 at 2048 (−0.32). This
reproduces established-fact A5 (SSC < vanilla on multi-key) on the real frozen model in this
environment, on mamba2-370m (long-gdn's number was GDN-1.3B; same pattern). +RM (residual memory) is
worse still. The single-needle win + multi-key regression is the exact gap the program must close.

## Mechanism localization — status (honest)
The goal was a per-hypothesis verdict (H1 pooling-collapse / H2 query-conditioning / …). Two attempts:

1. **from-scratch raw-dot probe (`lmr/analysis/descriptor_probe.py`, jobs 1610/1611) — INCONCLUSIVE.**
   A GDN-2 trained to solve MQAR (loss 0.018) showed **near-random** per-position AND pooled retrieval
   (pp_localize ~0.01, pool_chunk_hit ~0.11–0.22, decode ~0.01). Reason: it scored by **raw**
   `query·hidden` dot-product, which only reproduces the known "cos-sim is random" result (disc_ratio
   1.002); the multi-key signal lives in a **learned projection** (linear-probe AUC 0.92), invisible to
   raw geometry. So this probe cannot discriminate H1 vs H2 — methodological miss, not a model result.

2. **The valid mechanism test = learned-probe on the frozen SSC descriptors.** `diag_descriptor_probe.py`
   (long-gdn) does exactly this (LogisticRegression AUC on per-position vs pooled descriptors) and its
   imports (`lmr.adapters`, `lmr.readout`, `lmr.segment_runner`) resolve in this repo (eval_ruler uses
   them). Porting it to run on `mamba2-370m + ssc_370m.pt` is the next step — that gives per-hypothesis
   verdicts on the real frozen model.

## Hypothesis status (from established facts + this report)
| H | claim | status |
|---|---|---|
| — | SSC hurts multi-key on the real frozen model | **confirmed here** (0.452→0.129) |
| H1 | pooling collapses per-key identity | supported indirectly (AUC 0.92 binary vs retrieval 0.65; all-pooling-variants-failed A6); **direct per-position-vs-pooled learned-probe pending** |
| H2 | query-independent descriptor | supported (routing prob 0.19→0.028 with length A7); MaxSim test pending |
| H6 | layer specialization/aggregation | supported (single has clean layers, multi max 0.65 A7) |
| — | NOT within-chunk capacity | supported (rank analysis: within-chunk unsaturated, facts D2/E1) |

## Next
Port `diag_descriptor_probe` → run on mamba2-370m + `ssc_370m.pt` (learned per-position vs pooled AUC
for the queried key) → fills H1/H2 verdicts. Frozen GDN-1.3B replication when the campaign infra is
reachable.

## Reproduce
`env FLA_CONV_BACKEND=triton python -m lmr.scripts.eval_ruler --arch mamba2 --model
state-spaces/mamba2-370m --heads ckpt/ssc_370m.pt --variant ssc --low-rank-dim 64 --topk 2
--chunk-size 256 --tasks niah_single_1 niah_multikey_2 --lengths 2048 4096 --max-examples 50` (job 1614).
